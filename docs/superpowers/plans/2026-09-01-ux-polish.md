# 子项目 E · UX 收尾 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox syntax.

**Goal:** 四项独立 UX 改进：桌面通知、全局进度总览、失败诊断、待转换表多选。

**Spec:** `docs/superpowers/specs/2026-09-01-ux-polish-design.md`
**Worktree:** `/home/clawbot/workspace/audiobook-converter-ux`，分支 `feat/ux-polish`

---

### Task 1: 全局进度总览
- `renderJobs()` 时计算聚合：done 计 1，进行中按 progress 加权 → 侧栏「任务」标题行 `8/12 · 67%`；标签页标题 `(67%) Audiobook Converter`；无任务隐藏
- Test: 单元（聚合函数）；E2E（文本随任务更新）
- Commit `feat(ux): 全局进度总览`

### Task 2: 失败任务智能诊断
- jobCardHTML：failed 且 source_ids 不在注册表/书库 → 重试按钮禁用 + 「源文件已删除」提示；加 `jobsRetryable()` 判定
- Test: E2E（part4 删源场景后重试按钮禁用）
- Commit `feat(ux): 失败任务源丢失诊断`

### Task 3: 待转换表多选批量操作
- 表头 checkbox（全选/半选）+ 行首 checkbox；勾选 ≥1 → 操作条（已选 N · 移除所选 · 取消）
- 与拖拽/键盘重排共存（checkbox 列 nosort）
- Test: E2E（勾选 3 → 批量移除 → 计数/空状态）
- Commit `feat(ux): 待转换表多选批量操作`

### Task 4: 桌面通知
- `settings.notifications` 服务端持久化（PUT /api/settings）；侧栏任务标题行铃铛开关
- 触发：document.hidden && 任务进入 done/failed（SSE job.update）；Notification API，拒绝则开关降级提示
- Test: 单元（开关持久化走 API 已覆盖）；E2E（开关状态刷新后保持）
- Commit `feat(ux): 任务完成桌面通知`

### Task 5: 回归 + 收尾
- `uv run pytest` + E2E 全量；docs 更新；合并
